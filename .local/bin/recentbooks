#!/bin/sh

selected=$(\
        tac ~/.local/share/zathura/history | grep -Po '\[\K[^\]]*(.pdf|epub|mobi)' \
        | dmenu -i -l 10
        )

# exit if nothing is selected
[[ -z $selected ]] && exit

zathura "$selected"

exit 0

